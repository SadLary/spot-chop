# Nuxt Minimal Starter
Всю верстку сделал через Tailwind, просто ради практики.
Постарался в pixel-perfect.
В goods.mock.ts оставил комментарий насчет модели данных.
Make sure to install dependencies:

```bash
npm install

npm run dev
